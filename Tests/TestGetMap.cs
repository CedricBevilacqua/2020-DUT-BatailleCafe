using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Net.Sockets;


using Moteur.Initialisation;


namespace Tests
{
    [TestClass]
    public class TestGetMap
    {
        [TestMethod]
        public void TestConnector()
        {
            /*
            GetMap map = new GetMap();
            Socket connector = map.GetConnectStart("51.91.120.237", 1212);
            Assert.AreNotEqual(connector, null);
            byte[] mapTrame = map.GetConnectDownload(connector);
            Assert.AreNotEqual(mapTrame, null);
            */
        }
    }
}
